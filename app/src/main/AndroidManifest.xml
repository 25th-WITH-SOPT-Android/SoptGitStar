<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.greedy0110.soptgitstar">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".feature.git_repo_list.GitRepoListActivity"
            android:exported="true"/>
        <activity android:name=".feature.follower_list.FollowerListActivity"
            android:exported="true"/>
        <activity android:name=".feature.sign_up.SignUpActivity"
            android:exported="true"/>
        <activity android:name=".feature.sign_in.SignInActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>