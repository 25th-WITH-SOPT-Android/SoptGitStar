<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/containerListItemGitRepo"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    
    <ImageView
        android:id="@+id/imgListItemGitRepoOwner"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_margin="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:src="@drawable/dummy_avatar"
        />

    <TextView
        android:id="@+id/txtListItemGitRepoName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Activity1Fragment"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        app:layout_constraintStart_toEndOf="@id/imgListItemGitRepoOwner"
        app:layout_constraintTop_toTopOf="parent"
        android:textSize="14sp"
        android:textColor="#000000"
        />

    <TextView
        android:id="@+id/txtListItemGitRepoDesc"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        tools:text="Activity 와 Fragment 사이 데이터 교환을 위해 ViewModel + LiveData 를 사용한 기본적인 구현"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="20dp"
        app:layout_constraintStart_toStartOf="@id/txtListItemGitRepoName"
        app:layout_constraintTop_toBottomOf="@id/txtListItemGitRepoName"
        app:layout_constraintEnd_toEndOf="parent"
        android:textSize="11sp"
        android:textColor="#000000"
        />

</androidx.constraintlayout.widget.ConstraintLayout>